import { CAROUSELDETAILS } from '../shared/carouselDetails';

export const carouselDetails = (state = CAROUSELDETAILS, action) => {
    switch(action.type) {
        default:
            return state;
    }
}